# Core services
REDIS_URL=redis://redis:6379/0
QDRANT_URL=http://qdrant:6333
POSTGRES_DSN=postgresql://postgres:postgres@postgres:5432/dify
# If using Dockerized Ollama (default in compose), use service URL
OLLAMA_BASE_URL=http://ollama:11434

# Host directory containing Ollama models to mount into the container
# - Point this to a directory that already has models (e.g., copied from another machine's ~/.ollama)
# - If empty, the container will create a local ./.ollama folder next to compose and download there
OLLAMA_MODELS_HOST_DIR=.ollama

# Storage paths
STORAGE_DIR=/data/storage
SQLITE_PATH=/data/sqlite/ir.db
